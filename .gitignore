# ESP32 构建产物
build/
*.map
*.bin
*.elf
compile_commands.json
build.ninja
kconfig_menus.json

# 其他临时文件
.DS_Store
*.log
.vscode/settings.json  # 可选：排除本地VS Code配置
.devcontainer/*.log     # 可选：排除devcontainer日志
